#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;


int main(){
    // int a,b;
    // cin>>a>>b;
    // vector<int> arr(a);
    // for(int i=0;i<a;i++){
    //      cin>>arr[i];

    // }
    // sort(arr.begin(), arr.end());
    // double maxi=0;
    // maxi=max(maxi,(double)arr[0]-0);
    // maxi=max(maxi,(double)b-arr[a-1]);
    // for(int i=1;i<a;i++){
    //     maxi=max(maxi,(double)(arr[i]-arr[i-1])/2.0);
    // }
    // cout<<(double)maxi<<endl;
    int maxi=10/5.0;
    double x = maxi;
    cout<<x<<endl;

    return 0;

}